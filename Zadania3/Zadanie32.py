# Python 2.7.4

# L = L.sort() # Nie mozna przypisywac listy, ktora jednoczesnie sortujemy
# x, y = 1, 2, 3 # Brakuje trzeciej wartosci np.: z
# X = 1, 2, 3 ; X[1] = 4 # To nie jest konstrukcja z ktorej mozemy odczytywac wartosci elementow jak np.: lista
# X = [1, 2, 3] ; X[3] = 4 # Lista numerowana jest od 0 wiec nie ma elemntu trzeciego ktorego wartosc mozna odczytac.
# X = "abc" ; X.append("d") # "lancuchy zaliczane sa do niezmiennych sekwencji, czyli nie mozna ich zmieniac w obszarze przechowywania ich w pamieci."
# print map(pow, range(8)) # Funkcja pow pobiera dwa elementy a podajemy jej jeden.
